name: ci

on:
  pull_request:
    branches:
      - trunk

env:
  ZIG_VERSION: "0.13.0"

jobs:
  build:
    strategy:
      matrix:
        os:
          - ubuntu-22.04
          - windows-2022
        profile:
          - 'zig build'
          - 'zig build -Ddev'
          - 'zig build -Dturbo'
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4

    - name: Setup Zig
      uses: mlugg/setup-zig@v1
      with:
        version: ${{ env.ZIG_VERSION }}

    - name: Build
      run: ${{ matrix.profile }}
