dist: focal

language: c

install:
  - sudo apt-get install binutils-gold g++ cmakelibglew-dev mesa-common-dev build-essential libglew-dev libglm-dev mesa-utils libgl1-mesa-dev libgl1-mesa-glx libx11-dev libpng-dev

compiler:
  - clang
  - gcc

script:
  - ./sh/configure.sh
  - make clean all
