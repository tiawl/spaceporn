- check every payload capture => use pointer if necessary
- check ziggit doc for tips
- context module for each component
- use pointers into functions parameters
- use Uppercase for structs/enums/unions
- remove custom errors
- layers management: binding -> wrapper -> module
- change function names: convert_error () => convertError ()
- avoid relative path into @import: @import ("../log.zig") => @import ("log")
- change wrapper/module functions: destroy () -> deinit ()
- use std.fs.createFileAbsolute () instead of std.fs.cwd ().
- usage () and parse_options () should be Logger methods
- replace == by std.mem.eql () for string
