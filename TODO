- memchecks
- covchecks
- check configure.ac has deps
- check loadAtlas() and generateAtlas() with several depth
- Precompute flag
- Precompute and stop flag
- Redirect verbose to ~/.local/log/xteleskop to generate logs
